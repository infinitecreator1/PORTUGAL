{
  "name": "@imovel/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared contracts: zod schemas, provider interfaces, config, errors, hashing, pricing, feature taxonomy, fixtures.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./fixtures": {
      "types": "./src/fixtures.ts",
      "default": "./src/fixtures.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
