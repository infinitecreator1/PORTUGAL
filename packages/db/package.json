{
  "name": "@imovel/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Drizzle schema, SQL migrations with RLS, repositories, pg-boss job queue.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "generate": "drizzle-kit generate",
    "migrate": "tsx src/migrate.ts"
  },
  "dependencies": {
    "@imovel/core": "workspace:*",
    "drizzle-orm": "^0.38.2",
    "pg-boss": "^10.1.5",
    "postgres": "^3.4.5",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "drizzle-kit": "^0.30.1",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
